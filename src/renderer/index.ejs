<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta
      http-equiv="Content-Security-Policy"
      content="script-src 'self' 'unsafe-inline' 'unsafe-eval'"
    />
    <title>ApiChain</title>
  </head>
  <style type="text/css">
    /* 针对Webkit内核浏览器（Chrome, Safari, Edge等） */
    ::-webkit-scrollbar {
      width: 2px; /* 设置滚动条宽度为细线 */
    }

    ::-webkit-scrollbar-track {
      background: transparent; /* 滚动条轨道透明 */
    }

    ::-webkit-scrollbar-thumb {
      background: #000; /* 滚动条滑块为黑色 */
      border-radius: 1px; /* 可选：轻微圆角 */
    }

    /* 针对Firefox（64+版本） */
    html {
      scrollbar-width: thin; /* 细线模式 */
      scrollbar-color: #000 transparent; /* 滑块黑色，轨道透明 */
    }
  </style>
  <body style="margin: 0px; background-color: #2a2926;">
    <div id="root"><font color="white">Just a moment, the page will be displayed shortly...☕️🥚🍗</font></div>
  </body>
</html>
